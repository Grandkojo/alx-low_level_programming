# include "main.h"

/**
 * mul - Check dscription
 * Description: Multiplies two integers
 * @a: input character
 * @b: input character
 * Return: Product of the two numbers
 */
int mul(int a, int b)
{
	int product;

	product = (a * b);
	return (product);
}
