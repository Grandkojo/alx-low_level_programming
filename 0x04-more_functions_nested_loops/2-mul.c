# include "main.h"

/**
 * mul - Check dscription
 * Descriptio: Multiplies two integers
 * @a: input character
 * @b: input character
 * Return: Product of the two numbers
 */
int mul(int a, int b)
{
	int product;

	product = (a * b);
	return (product);
}
