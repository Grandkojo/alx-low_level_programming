#include <stdio.h>

/**
 * main - Check code
 * Description: prints the name of the file it was compiled with
 * Return: Always 0 Success
 */
int main(void)
{
	printf("%s\n", __FILE__);
	return (0);
}
